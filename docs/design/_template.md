<!--
画面設計書のテンプレート。新しい画面ファイルを作るときはこの内容をコピーする。

ファイル名の決め方（2026-06-22決定、一覧・新規作成・編集・削除が1ファイルに混在すると読みづらいとのユーザー指摘により分割）:
- 一覧・新規作成（Dialog/別ページ）・編集（Dialog/別ページ）・削除（確認）のうち2つ以上が存在する画面は
  docs/design/{画面名}-list.md / {画面名}-create.md / {画面名}-edit.md / {画面名}-delete.md に分割する
  （例: categories-list.md, categories-create.md, categories-edit.md, categories-delete.md）
- 編集Dialogが新規作成Dialogと同形式（初期値のみ異なる）の場合、{画面名}-edit.mdは
  {画面名}-create.mdを参照し、差分（初期値・対象・追加項目）のみを記載する薄いドキュメントにしてよい
- CRUDの概念がそもそも存在しない単一画面（home.md, ai.md, landing.md, profile-setup.md等）は
  従来通り docs/design/{画面名}.md の1ファイルのままでよい（分割しない）
- Dialog/AlertDialogの「見た目の共通フレームワーク」（タイトル+×アイコンの配置、フッターのボタン配置等、
  画面をまたいで共通するパターン）は modals.md に残し、{画面名}-create/edit/delete.mdからリンクで参照する。
  各CRUDファイルにはその画面固有のフォーム項目・業務ロジックのみを記載し、共通フレームワークの説明を重複させない
- 作成後は docs/design/README.md の「画面一覧」表にリンクを追加すること
-->

# {画面名}

[機能仕様](../specs/features/{feature}.md)に対応する画面（`{ルートパス}`）。

## 関連画面

| 遷移元 | 遷移先 |
|---|---|
| 例: 下部固定ナビゲーション（どこからでも） | 例: `/transactions/new`（「+取引を追加」FAB） |

全体の遷移図は[architecture/screen-flow.md](../architecture/screen-flow.md)を参照。ここには「この画面に直接関わる遷移」のみ記載する。

## 関連API

| メソッド | パス | 用途 |
|---|---|---|
| GET | `/api/{resource}` | 一覧取得 |

詳細な仕様（リクエスト/レスポンス・バリデーション）は[機能仕様](../specs/features/{feature}.md#api)を参照。ここでは「この画面がどのAPIを呼ぶか」の対応関係のみ記載する。

## 採番済みスクリーンショット

採番は `docs/design/screenshots/{screen}-{device}-numbered.png`（Pillowで番号ピンを描画したもの）を参照する。元画像（番号なし）は `docs/design/screenshots/{screen}-{device}.png`。

### PC版

![{画面名} PC版](./screenshots/{screen}-pc-numbered.png)

Stitch Screen ID: `screens/{id}`

### SP版

![{画面名} SP版](./screenshots/{screen}-sp-numbered.png)

Stitch Screen ID: `screens/{id}`

### 状態パターン（タブ切替・フィルタ適用等）

デフォルト状態以外に、タブ・ラジオボタン切替やフィルター適用後で表示が変わる場合はここに追加する（[style-guide.mdの画面パターン・状態デザインの方針](./style-guide.md#画面パターン状態デザインの方針2026-06-22決定)参照）。パターンがない画面はこの節自体を省略してよい。

#### {パターン名}（例: 「定期取引」タブ）

![{画面名} {パターン名}](./screenshots/{screen}-{device}-{状態名}-numbered.png)

Stitch Screen ID: `screens/{id}`

変更点: 例:「一覧」タブから「定期取引」タブに切り替えた状態。テーブル列が「頻度」「実行日」に変わる。

## パーツ一覧

| No | 名称 | 説明 | 遷移先・挙動 |
|---|---|---|---|
| ① | | | |
| ② | | | |
| ③ | | | |

状態パターンに固有のパーツがある場合は、デフォルト状態からの連番継続でも、パターンごとに別の番号体系（例: P1, P2…）に分けてもよい。画面の状態数・パーツの重複度に応じて分かりやすい方を選ぶ。

## 状態一覧

正常表示以外の状態をここにまとめる。スクリーンショットがない状態は文章で説明する。**タブ切替・フィルタ適用等の状態パターンは上記の[状態パターン](#状態パターンタブ切替フィルタ適用等)節にスクリーンショット付きで記載済みのため、ここでは空状態・エラー状態・ローディング状態など「スクリーンショットを作らない/作れない状態」のみ記載する。**

| 状態 | 表示内容 |
|---|---|
| 空状態 | 例:「この月の取引はまだありません」+「取引を追加」ボタン |
| エラー状態 | 例: API失敗時のトースト文言・再試行手段 |
| ローディング状態 | 例: スケルトン表示の範囲、初回読み込みとリフレッシュの違い |

## レスポンシブ差分

PC版・SP版でレイアウト以外に挙動が異なる箇所のみ記載する（単純な折り返し・幅の違いは記載不要）。

- 例: SP版はメンバー切り替えにスワイプ操作が追加される（PC版は矢印クリックのみ）

## 採用した方向性

- なぜこのレイアウト・配色にしたか、仕様のどの記述に対応しているかを箇条書きで記載する
- 仕様書への参照は `[xxx.md](../specs/features/xxx.md#見出し)` の形式でリンクする

## 既存実装との差分

未実装の場合は「未実装のため差分なし。」と記載する。再設計の場合は実装済みコンポーネントとの差分を記載する。

## 仕様外要素（実装時は無視すること）

Stitchが仕様に存在しない要素を独自に追加した場合に記載する。なければ「特になし。」と記載する。

## 更新履歴

| 日付 | 変更内容 |
|---|---|
| YYYY-MM-DD | 初版作成 |
